# MEAN-RSVP-Auth0

## Development server

```
$ ng serve
$ NODE_ENV=dev nodemon server
```

Available on `http://localhost:4200/`. 

## Build

```
$ ng build --prod
$ node server
```

Available on `http://localhost:8083`.
